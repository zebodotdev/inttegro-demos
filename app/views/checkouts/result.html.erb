<main class="result"><span class="resultMark <%= "neutral" unless @complete %>"><%= @complete ? "✓" : "×" %></span><p class="eyebrow"><%= @complete ? "BROWSER RETURNED" : "CHECKOUT CANCELED" %></p><h1><%= @complete ? "Payment is being verified." : "No completion was claimed." %></h1><p><%= @complete ? "The redirect is not proof of payment. Fulfill only from authoritative server-side status." : "The customer can safely begin a fresh checkout attempt." %></p><%= link_to "Return to the demo", root_path %></main>
