{
  "name": "inttegro-demo-express",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/src/server.js",
    "deploy:cloudflare": "wrangler deploy",
    "cf-typegen": "wrangler types --env-interface CloudflareEnv worker-configuration.d.ts",
    "test": "tsx --test test/*.test.ts",
    "check": "npm run build && npm test"
  },
  "dependencies": {
    "@inttegro/inttegro-sdk": "7.0.0",
    "express": "5.2.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^24.0.0",
    "tsx": "4.23.13",
    "typescript": "7.0.2",
    "wrangler": "4.129.0"
  },
  "engines": { "node": ">=24" },
  "cloudflare": {
    "bindings": {
      "INTTEGRO_API_KEY": {
        "description": "Create a dedicated API key in Inttegro Studio. This value remains server-side."
      },
      "INTTEGRO_DEMO_PRODUCT_ID": {
        "description": "The active Afterglow admission Product ID resolved by the server at checkout."
      },
      "INTTEGRO_DEMO_PRICE_ID": {
        "description": "An active Price ID belonging to the configured Afterglow Product."
      },
      "INTTEGRO_DEMO_PUBLIC_URL": {
        "description": "Optional public origin for completion and cancellation URLs, without a trailing slash."
      }
    }
  }
}
